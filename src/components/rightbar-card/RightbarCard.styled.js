import styled from "styled-components";

export const StyledRightbarCard = styled.div`
  border-bottom: 2px var(--color-border) solid;
  display: flex;
  flex-direction: column;
  gap: .5rem;
  padding: 1rem 0;

  h3 {
    font-weight: 500;
    font-size: .9rem;
  }
`;
